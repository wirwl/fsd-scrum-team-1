import { call } from 'redux-saga/effects';

import Api from '@/services/Api';

import { FetchBookedRoomsAction } from './types';

const api = new Api();

function* fetchBookedRoomsSaga(action: FetchBookedRoomsAction) {
  try {
    const userId = storage
  } catch (error) {
    
  }
}
